[project]
name = "agent-lab-method"
version = "0.1.0"
description = "Minimal read-only method package for synthetic GenMentor run artifacts"
requires-python = ">=3.11"
dependencies = ["pydantic>=2.7,<3"]

[project.optional-dependencies]
test = ["pytest>=8,<9"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
